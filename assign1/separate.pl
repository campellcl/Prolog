/* sperate.pl */
/* Takes a list as input paremeter and two lists as output parameters.
    The incoming list is sepreated into the two output lists. */
/*base case for separate empty list: */
separate([],[] | []).
