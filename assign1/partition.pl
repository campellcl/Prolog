/* partition.pl */
/* 2 input parameters and 2 output parameters. */
/* An input list of integers and an input integer value, two ouput
    lists of integers; one containing original list values <= the 
    input value, and the other output list contains list values > than
    input value. */
